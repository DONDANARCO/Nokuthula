{
  "name": "footmed-podiatrist-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site with Vercel serverless form backend",
  "scripts": {
    "dev": "vercel dev"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.916.0",
    "@aws-sdk/lib-dynamodb": "^3.916.0"
  }
}
